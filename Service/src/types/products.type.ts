export interface IProducts {
    id: number,
    name: string,
    price: number,
    originalPrice: number,
    image: string,
    category: string
    rating: number,
    reviews: number
    isNew: boolean
    isSale: boolean
}